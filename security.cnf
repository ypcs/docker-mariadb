;
; Enable audit logging
;
; See <https://mariadb.com/kb/en/mariadb-audit-plugin-configuration/>
;
[mariadb]
plugin_load_add = server_audit
server_audit=FORCE_PLUS_PERMANENT

[server]
server_audit_logging = ON
